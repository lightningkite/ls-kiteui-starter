org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

android.useAndroidX=true
android.defaults.buildfeatures.buildconfig=true
android.nonTransitiveRClass=false
android.nonFinalResIds=false

kotlin.code.style=official

kfc.source.maps=true
kfc.strict.mode=false

# KSP 2.3.10's K2 backend has an unbounded-recursion StackOverflowError in
# IncrementalContextAA.filePathFor() when a KClass-valued annotation argument
# (@References(X::class), here) triggers a real incremental regeneration. Not
# fixable by raising -Xss. See ~/Projects/metawork/tmp/AGENT-BRIEF.md #3.
ksp.incremental=false
